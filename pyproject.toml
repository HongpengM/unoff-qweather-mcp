[project]
name = "unoff-qweather-mcp"
version = "0.1.0"
description = "An unofficial MCP (Multiagent Conversation Protocol) wrapper for the QWeather API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx[socks]>=0.28.1",
    "mcp[cli]>=1.7.0",
]
